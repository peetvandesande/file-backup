# file-backup
Docker image to backup files into an archive.
Defaults to using its cron entry or can be run manually.

See https://hub.docker.com/repository/docker/peetvandesande/file-backup/general for information.
